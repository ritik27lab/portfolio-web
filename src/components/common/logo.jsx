import React from "react";

import "./styles/logo.css";

const Logo = (props) => {
	return (
		<React.Fragment>
			{/* {link ? <Link to="/">{imageElement}</Link> : imageElement} */}
		</React.Fragment>
	);
};

export default Logo;
